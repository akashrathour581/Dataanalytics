# DataSphere frontend

See the [project README](../README.md) for setup, routes, configuration, architecture and verification.

Run `npm run dev` for development, `npm run build` for the static build and SEO pages, `npm run lint` and `npm test` for checks.
